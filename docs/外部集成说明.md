# 外部集成说明

本练习模拟一个“客户中心”外部服务。

mock 服务地址：

`http://localhost:9090/mock/customer-center/customers/{customerCode}`

返回可能有三类：

1. 正常
2. 字段缺失
3. 服务异常
4. HTTP 成功但 `data` 为空

训练要求：

- 不要把外部字段直接透传到前端
- 要做字段映射
- 要做错误处理
- 同步成功后要刷新列表中的当前行状态
